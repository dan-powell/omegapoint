all:
  hosts:
    production:
      ansible_host: # example.com
      ansible_port: # 22
      ansible_user: # user
      domain: "omegapoint.city"
      # Add any additional domains here including www, comment out if no other domains required
      other_domains: 
        - "*.omegapoint.city"
      cloudflare_email: 
      cloudflare_api_key: 